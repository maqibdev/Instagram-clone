<% if story.errors.any? %>
    <div id="error_explanation">

      <h2>
        <%= pluralize(story.errors.count, "error") %>
        prohibited this Storty from being saved:
      </h2>

      <br>

      <ul>
        <%story.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>

    </div>
<% end %>
